int x, y;
float z;
float r1, r2, r3;
str player;

func int main()
{
    str opponent;
    float t1, t2;
    float temp;

    player = "CreeperBuddy" "next";
    opponent = "ENEMY_";

    x = 53 + 7;
    y = x / 2;
    z = y - (x * x) * 4.7;

    t1 = (x * y) * z;
    temp = 225;
    t2 = t1 * 5;

    cmd "kill @e" {t1 + t2};

    while (temp > 20)
    {
        temp = t2 - (t2 / 2);
    }

    if (t1 > t2)
    {
        cmd "say t1 > t2" "natalie rossello";
    }
    elif (t1 == t2)
    {
        while (t1 < t2)
        {
            t1 = t1 + 1;
        }

        cmd "say t1 == t2";
    }
    else
    {
        cmd "say idit" {x} "DUMB\"" {player} "ILLEGAL ARGS";
    }

    return 5;
}

func int otherFunc(int x1, int y1)
{
    return x1 - y1;
}